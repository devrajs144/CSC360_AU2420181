**Lecture Date:** 13/07/2026

Today's session was more hands-on compared to the earlier lectures, which had mostly been conceptual. Most of the class went into setting up tools that we'll likely be using throughout the rest of the course, so this felt like a practical continuation of the SSH and Git-related discussion from the previous lecture. The first step involved installing Apache Maven, which is a build automation tool for Java projects. While the lecture didn't go deep into Maven's internals, having it installed and ready suggests that upcoming assignments will involve project structures that rely on it for managing dependencies and builds, so this is worth keeping in mind going forward.

Following the setup, we wrote a basic Java file to draw closed shapes. This was a good way to move from the earlier discussion on geometric primitives, lines and curves, into actual implementation. Seeing a closed shape being constructed in code made the earlier theoretical explanation of primitives feel more concrete, since a closed shape is essentially just a combination of connected lines or curves forming a boundary. This is something I want to revisit once we get into more complex shapes, since understanding how a simple closed shape is built should make it easier to follow more advanced constructions later.

The lecture also involved installing TortoiseGit, which is a graphical tool for performing Git operations. This connects directly to the previous class on SSH and HTTPS for accessing Git repositories, since TortoiseGit will likely be the interface through which those concepts get applied practically. Having a GUI for Git operations should make version control more approachable, especially compared to purely command-line based workflows, though it will be useful to understand what's happening underneath the interface as well.

Lastly, we installed and set up Java-related extensions within Antigravity. This part of the lecture was more about getting the development environment fully functional rather than introducing new theoretical content, but it's an important step since a properly configured environment is what will allow the actual coding exercises in this course to run smoothly.

Overall, this lecture felt like a transition point from theory into practice. The tools installed today, Maven, TortoiseGit, and the Java extensions, are all pieces that will likely come together as the course moves into building actual graphics programs. The most useful realization from today was seeing how the earlier abstract concept of primitives translated into an actual piece of working code, which made the connection between theory and practice much clearer.
